define(function(require) {

  var Camera = function() {
    
  };
  
  Camera.prototype.onInit = function() {
  };
  
  Camera.prototype.viewToModel = function(view) {
  };
  
  Camera.prototype.modelToView = function(model) {
  };
  
  return Camera;
});