define(['backbone', 'jquery', 'underscore'],
function(backbone, $, _) {
    return {
        run : function() {
            console.log(backbone);
            console.log("SYSTEM: Main application running...");
        }
    };
});